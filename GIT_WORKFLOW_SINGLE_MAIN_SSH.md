# BThwani — Git Workflow (Single Main, SSH)

## 📋 السياسة المعتمدة

**مسار عمل بسيط جداً:**
- ✅ مطوّر واحد فقط
- ✅ فرع وحيد للعمل اليومي: `main`
- ✅ اتصال بـ GitHub عبر SSH
- ✅ لا Pull Requests، لا حوكمة ثقيلة، لا rebase في الروتين اليومي
- ✅ مسار واضح وبسيط: `add` → `commit` → `push`

---

## 1) التحقق من حالة المشروع الحالية

```bash
# التحقق من المسار الحالي
pwd

# التحقق من حالة Git
git status

# التحقق من الفروع المحلية
git branch

# التحقق من Remote الحالي
git remote -v

# التحقق من SSH مع GitHub
ssh -T git@github.com
```

---

## 2) تجهيز SSH وربط الريبو بـ SSH

### 2.1 التحقق من وجود SSH Key

```bash
# التحقق من وجود SSH keys
ls -la ~/.ssh

# إذا لم يكن موجوداً، إنشاء SSH key جديد
ssh-keygen -t ed25519 -C "your_email@example.com"
# اضغط Enter للقيم الافتراضية

# عرض المفتاح العام لإضافته إلى GitHub
cat ~/.ssh/id_ed25519.pub
```

### 2.2 إضافة SSH Key إلى GitHub

1. انسخ محتوى `~/.ssh/id_ed25519.pub`
2. اذهب إلى: https://github.com/settings/keys
3. اضغط "New SSH key"
4. الصق المفتاح واحفظه

### 2.3 تغيير Remote من HTTPS إلى SSH

```bash
# إزالة Remote الحالي (HTTPS)
git remote remove origin

# إضافة Remote جديد (SSH)
git remote add origin git@github.com:bthwani1/bthwani-platform.git

# التحقق من التغيير
git remote -v

# اختبار الاتصال
ssh -T git@github.com
```

---

## 3) تنظيف الفروع (من GitHub ومن الجهاز)

### 3.1 حفظ التغييرات الحالية أولاً

```bash
# حفظ التغييرات الحالية
git add .
git commit -m "chore: save current work before branch cleanup"
```

### 3.2 الانتقال إلى فرع main

```bash
# جلب آخر تحديثات من origin
git fetch origin

# الانتقال إلى main
git checkout main

# تحديث main من origin
git pull origin main
```

### 3.3 دمج التغييرات من فرع آخر إلى main (إن أردت)

```bash
# دمج التغييرات من فرع آخر إلى main
git merge <اسم-الفرع>
```

### 3.4 عرض الفروع المحلية

```bash
# عرض جميع الفروع المحلية
git branch
```

### 3.5 حذف فرع محلي معيّن

```bash
# حذف فرع محلي (استبدل <اسم-الفرع> باسم الفرع الفعلي)
git branch -D <اسم-الفرع>

# أمثلة:
# git branch -D feat/oas-servers-contact
# git branch -D feat/bootstrap-guides
```

### 3.6 عرض الفروع البعيدة

```bash
# عرض جميع الفروع البعيدة
git branch -r
```

### 3.7 حذف فرع بعيد معيّن (من GitHub)

```bash
# حذف فرع بعيد (استبدل <اسم-الفرع> باسم الفرع الفعلي)
git push origin --delete <اسم-الفرع>

# أمثلة:
# git push origin --delete feat/oas-servers-contact
# git push origin --delete feat/bootstrap-guides

# تنظيف مراجع الفروع البعيدة المحذوفة
git remote prune origin
```

### 3.8 التحقق من النتيجة

```bash
# التحقق من الفروع المحلية (يجب أن يكون main فقط)
git branch

# التحقق من الفروع البعيدة (يجب أن يكون main فقط)
git branch -r

# التحقق من الحالة النهائية
git status
```

---

## 4) ورقة غش (Daily Cheat Sheet) - المسار الرسمي

### 4.1 قبل البدء (كل يوم)

```bash
git checkout main
git pull origin main
```

### 4.2 بعد التعديلات

```bash
git status
git add .
git commit -m "شرح مختصر للتغيير"
git push origin main
```

---

## 5) (اختياري) فرع تجربة كبيرة

### 5.1 إنشاء فرع تجريبي

```bash
git checkout main
git pull origin main
git checkout -b exp/big-change
```

### 5.2 العمل على الفرع التجريبي

```bash
# بعد التعديلات
git add .
git commit -m "exp: وصف التغييرات التجريبية"
git push -u origin exp/big-change
```

### 5.3 دمج الفرع التجريبي في main عندما تقتنع

```bash
git checkout main
git pull origin main
git merge exp/big-change
git push origin main
```

### 5.4 حذف الفرع التجريبي بعد الدمج

```bash
# حذف محلي
git branch -D exp/big-change

# حذف من GitHub
git push origin --delete exp/big-change
```

---

## 6) أوامر مفيدة إضافية

```bash
# عرض الفرق قبل الـ commit
git diff

# عرض الفرق للملفات المضافة
git diff --cached

# إلغاء إضافة ملف
git restore --staged <file>

# إلغاء التغييرات في ملف
git restore <file>

# عرض آخر commit
git show

# عرض سجل الـ commits
git log --oneline -10

# البحث في سجل الـ commits
git log --grep="كلمة البحث"
```

---

## 7) التعامل مع الأخطاء الشائعة

```bash
# إذا رفض push بسبب divergence (تضارب)
git pull origin main
git push origin main

# إذا أردت إلغاء آخر commit (مع الاحتفاظ بالتغييرات)
git reset --soft HEAD~1

# إذا أردت إلغاء آخر commit (مع حذف التغييرات)
git reset --hard HEAD~1

# إذا علّق git في rebase (استثناء فقط)
git rebase --abort
```

---

## 8) (متقدّم/اختياري) git stash

**ملاحظة:** المسار الرسمي لحفظ العمل هو `git add .` → `git commit -m "..."` → `git push origin main`

إذا احتجت `git stash` (غير موصى به في المسار اليومي):

```bash
# حفظ التغييرات مؤقتاً
git stash

# عرض التغييرات المحفوظة
git stash list

# استرجاع التغييرات المحفوظة
git stash pop

# حذف التغييرات المحفوظة
git stash drop
```

---

## 9) التحقق النهائي

بعد تنفيذ جميع الأوامر، تحقق من:

```bash
# 1. Remote يجب أن يكون SSH
git remote -v
# يجب أن يظهر: git@github.com:bthwani1/bthwani-platform.git

# 2. يجب أن تكون على فرع main
git branch
# يجب أن يظهر: * main

# 3. يجب أن يكون main فقط (محلي وبعيد)
git branch -a
# يجب أن يظهر: main و origin/main فقط

# 4. الحالة يجب أن تكون نظيفة
git status
# يجب أن يظهر: "nothing to commit, working tree clean"

# 5. SSH يجب أن يعمل
ssh -T git@github.com
# يجب أن يظهر: "Hi bthwani1! You've successfully authenticated..."
```

---

## ⚠️ ملاحظات مهمة

1. **مسار بسيط** - `add` → `commit` → `push` فقط
2. **لا rebase** - لا تستخدم `git rebase` في الروتين اليومي
3. **لا stash** - استخدم `commit` بدلاً من `stash` لحفظ العمل
4. **فرع واحد** - `main` هو الفرع الوحيد للعمل اليومي
5. **SSH فقط** - اتصال مع GitHub عبر SSH

---

## 📝 سجل التغييرات

بعد تنفيذ الأوامر، سجل هنا ما تم:

- [ ] تم التحقق من الحالة الحالية
- [ ] تم تجهيز SSH
- [ ] تم تغيير Remote إلى SSH
- [ ] تم الانتقال إلى main
- [ ] تم تنظيف الفروع
- [ ] تم التحقق النهائي

---

**آخر تحديث:** 2025-01-XX
