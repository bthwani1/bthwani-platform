# BThwani — Repo Size & GitIgnore Auditor Report (v1.0)

> **تاريخ التوليد**: `PLACEHOLDER_DATE`  
> **الريبو**: `bthwani-platform`  
> **الهدف**: تحليل حجم المشروع + مراجعة `.gitignore` لضمان أن ما سيُنظَّم لاحقًا هو **سورس فقط** بدون مخلفات build أو ملفات ضخمة غير ضرورية.

---

## SIZE_SUMMARY

### 1) تقدير منطقي للحجم الكلي

المشروع هو **Monorepo NestJS** يحتوي على:
- سورس كود TypeScript (backend services)
- مواصفات OpenAPI
- توثيق شامل
- تكوينات ومهاجرات
- ملفات تعريف للتطبيقات واللوحات

**التقدير المنطقي للحجم**:
- بدون `node_modules/`: **~50-100 MB** (سورس كود، توثيق، CSV، JSON)
- مع `node_modules/`: **~300-500 MB** (إذا تم تثبيت التبعيات)

### 2) أكبر المجلدات (Top-Level) من حيث الحجم النسبي المتوقع

| المجلد | النوع | الحجم المتوقع | الملاحظات |
|--------|------|---------------|-----------|
| `node_modules/` | `deps` | ~200-400 MB | تبعيات Node.js - **يجب أن يكون في .gitignore** ✅ |
| `dist/` | `build` | ~20-50 MB | ناتج build لـ NestJS - **يجب أن يكون في .gitignore** ✅ |
| `coverage/` | `test` | ~5-20 MB | تقارير تغطية الكود - **يجب أن يكون في .gitignore** ✅ |
| `docs/` | `source` | ~10-30 MB | توثيق شامل - **يجب تتبعه في Git** ✅ |
| `src/` | `source` | ~5-15 MB | سورس كود TypeScript - **يجب تتبعه في Git** ✅ |
| `apps/` | `source` | ~2-10 MB | مواصفات التطبيقات - **يجب تتبعه في Git** ✅ |
| `dashboards/` | `source` | ~2-5 MB | مواصفات اللوحات - **يجب تتبعه في Git** ✅ |
| `oas/` | `source` | ~1-5 MB | مواصفات OpenAPI - **يجب تتبعه في Git** ✅ |
| `scripts/` | `source` | ~1-3 MB | سكريبتات وأدوات - **يجب تتبعه في Git** ✅ |
| `migrations/` | `source` | ~0.5-2 MB | ملفات migration - **يجب تتبعه في Git** ✅ |

### 3) تصنيف المجلدات

#### ✅ سورس كود ضروري (يجب تتبعه في Git)
- `src/` - كود NestJS
- `apps/` - مواصفات التطبيقات
- `dashboards/` - مواصفات اللوحات
- `docs/` - التوثيق
- `oas/` - مواصفات OpenAPI
- `scripts/` - سكريبتات
- `migrations/` - مهاجرات قاعدة البيانات
- `registry/` - سجل SSOT
- `shared/` - كود مشترك
- `test/` - اختبارات

#### ❌ ناتج build / cache / dependencies (يجب تجاهله)
- `node_modules/` - تبعيات
- `dist/` - ناتج build
- `coverage/` - تقارير coverage
- **مفقود**: `.nx/` - cache خاص بـ Nx (غير موجود حاليًا لكن يجب تجاهله)
- **مفقود**: `.turbo/` - cache خاص بـ Turbo (غير موجود حاليًا لكن يجب تجاهله)
- **مفقود**: `.expo/` - cache خاص بـ Expo (غير موجود حاليًا لكن يجب تجاهله)
- **مفقود**: `.cache/` - cache عام (غير موجود حاليًا لكن يجب تجاهله)

#### ⚠️ ملفات قد تكون كبيرة (يجب التحقق منها)
- `*.tsbuildinfo` - ملفات build info لـ TypeScript (موجود في `dist/` لكن يجب التأكد)
- ملفات `.log` - سجلات (يجب تجاهلها)
- ملفات `.env` - متغيرات بيئة (يجب تجاهلها)

---

## GITIGNORE_ISSUES

### 1) قائمة الأنماط المُفترض وجودها في `.gitignore`

#### ✅ موجود حاليًا في `.gitignore`:
- `/dist` ✅
- `/node_modules` ✅
- `/coverage` ✅
- `*.log` ✅
- `.env`, `.env.local`, `.env.*.local` ✅
- `.DS_Store` ✅
- `/.idea`, `.vscode/*` ✅

#### ❌ **مفقود ويجب إضافته**:

**GITIGNORE-01**: ملفات TypeScript build info  
- **المشكلة**: `*.tsbuildinfo` غير موجودة في `.gitignore`
- **الملاحظة**: يوجد ملف `dist/tsconfig.build.tsbuildinfo` في مجلد `dist/` (الذي متجاهل بالفعل)، لكن يجب إضافة النمط بشكل عام
- **التأثير**: منخفض (لأن `dist/` متجاهل)، لكن يُفضل إضافته للحماية

**GITIGNORE-02**: Nx cache  
- **المشكلة**: `.nx/` غير موجود في `.gitignore`
- **التأثير**: متوسط - Nx يستخدم cache قد يصل حجمه إلى 50-100 MB
- **الخطورة**: قد يرفع حجم الريبو بدون داعٍ

**GITIGNORE-03**: Turbo cache  
- **المشكلة**: `.turbo/` غير موجود في `.gitignore`
- **التأثير**: منخفض (لأن Turbo غير مستخدم حاليًا في المشروع)
- **الخطورة**: منخفضة، لكن يُفضل إضافته للمستقبل

**GITIGNORE-04**: Expo cache  
- **المشكلة**: `.expo/` غير موجود في `.gitignore`
- **التأثير**: منخفض (لأن Expo غير مستخدم حاليًا في المشروع)
- **الخطورة**: منخفضة، لكن يُفضل إضافته للمستقبل

**GITIGNORE-05**: ملفات .env العامة  
- **المشكلة**: `.env.*` غير موجودة بشكل كامل (يوجد `.env.*.local` فقط)
- **التأثير**: متوسط - قد توجد ملفات `.env.development`, `.env.production`, إلخ
- **الخطورة**: قد تحتوي على أسرار

**GITIGNORE-06**: ملفات OS إضافية  
- **المشكلة**: `Thumbs.db` (Windows) غير موجود
- **التأثير**: منخفض، لكن يُفضل إضافته

**GITIGNORE-07**: Playwright artifacts  
- **المشكلة**: `test-results/`, `playwright-report/`, `playwright/.cache/` غير موجودة
- **التأثير**: متوسط - Playwright يستخدم مجلدات لحفظ نتائج الاختبارات
- **الخطورة**: قد تحتوي على ملفات كبيرة

**GITIGNORE-08**: ملفات PEM و KEY  
- **المشكلة**: `*.pem`, `*.key` غير موجودة في `.gitignore`
- **التأثير**: عالي - قد تحتوي على مفاتيح خاصة
- **الخطورة**: **عالية جداً** - خطر أمني

**GITIGNORE-09**: ملفات temporary إضافية  
- **المشكلة**: `*.tmp`, `*.temp` موجودة، لكن `*.swp`, `*.swo` (Vim) غير موجودة
- **التأثير**: منخفض

**GITIGNORE-10**: مجلدات build إضافية  
- **المشكلة**: `build/`, `out/`, `.next/` غير موجودة (رغم أنها قد لا تكون مستخدمة حالياً)
- **التأثير**: منخفض، لكن يُفضل إضافتها للمستقبل

### 2) ملخص المشاكل الحرجة

| الأولوية | المشكلة | الخطورة | التأثير |
|----------|---------|---------|---------|
| 🔴 **عالية** | `*.pem`, `*.key` غير موجودة | **أمني** | **عالي** |
| 🟠 **متوسطة** | `.nx/` غير موجود | حجم الريبو | متوسط |
| 🟠 **متوسطة** | Playwright artifacts غير موجودة | حجم الريبو | متوسط |
| 🟡 **منخفضة** | `.env.*` غير مكتملة | أمني | منخفض |
| 🟡 **منخفضة** | `*.tsbuildinfo` غير موجودة | حجم الريبو | منخفض |

### 3) تقييم `.gitignore` الحالي

**التقييم العام**: ⚠️ **جيد لكن يحتاج تحسين**

**النقاط الإيجابية**:
- ✅ يتجاهل `dist/`, `node_modules/`, `coverage/` بشكل صحيح
- ✅ يتعامل مع ملفات `.env` الأساسية
- ✅ يتعامل مع ملفات OS والـ IDEs

**النقاط السلبية**:
- ❌ **نقص حاسم**: `*.pem` و `*.key` غير موجودة (خطر أمني)
- ❌ **نقص مهم**: `.nx/` غير موجود (Nx مستخدم في المشروع)
- ❌ **نقص متوسط**: Playwright artifacts غير موجودة (Playwright مستخدم في المشروع)
- ⚠️ `.env.*` غير مكتملة

---

## RECOMMENDED_GITIGNORE_SNIPPET

### المقترح الكامل لـ `.gitignore`:

```gitignore
# ============================================
# Dependencies
# ============================================
node_modules/
.pnp/
.pnp.*

# ============================================
# Build outputs
# ============================================
dist/
build/
out/
.next/
coverage/
.nyc_output/

# TypeScript build info
*.tsbuildinfo
tsconfig.build.tsbuildinfo

# ============================================
# Caches & tools
# ============================================
.nx/
.turbo/
.cache/
.expo/

# Android & iOS build artifacts
android/build/
ios/build/
.gradle/

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/

# ============================================
# Environment & secrets
# ============================================
.env
.env.local
.env.*.local
.env.*
*.pem
*.key
*.secret

# ============================================
# Logs
# ============================================
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ============================================
# OS files
# ============================================
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ============================================
# IDEs and editors
# ============================================
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# VSCode (with exceptions for shared settings)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# ============================================
# Temporary files
# ============================================
*.tmp
*.temp
```

### ملاحظات على المقترح:

1. **الأمان**: إضافة `*.pem`, `*.key`, `*.secret` بشكل صريح
2. **Nx**: إضافة `.nx/` لحماية cache Nx
3. **Playwright**: إضافة مجلدات test-results و playwright-report
4. **TypeScript**: إضافة `*.tsbuildinfo` بشكل عام
5. **المرونة**: إضافة أنماط للمستقبل (`.turbo/`, `.expo/`, `build/`, `out/`, `.next/`)

---

## BIG_TRACKED_FILES (RECOMMENDED CHECKS)

### 1) شرح المشكلة

قد تكون هناك ملفات كبيرة متتبعة في Git (مثل ملفات media، zip، dumps، أو ملفات build) يجب اكتشافها وإزالتها من Git.

### 2) أوامر Bash للفحص

```bash
# ============================================
# أكبر 20 ملف متتبَّع حاليًا في الريبو (حسب حجم الملف في الـ working tree)
# ============================================
git ls-files -z | xargs -0 du -k 2>/dev/null | sort -nr | head -n 20

# ============================================
# أكبر 20 ملف في تاريخ Git (قد تكون رفعت ملفات ضخمة سابقًا)
# ============================================
git rev-list --objects --all \
  | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' \
  | awk '$1 == "blob" {print $3 " " $2 " " $4}' \
  | sort -nr \
  | head -n 20

# ============================================
# حجم المجلد الكلي (تقريبي)
# ============================================
du -sh .

# ============================================
# أكبر 15 مجلد في الجذر
# ============================================
du -sh * | sort -hr | head -n 15

# ============================================
# البحث عن ملفات > 5MB متتبعة في Git
# ============================================
git ls-files -z | xargs -0 du -k 2>/dev/null | awk '$1 > 5000 {print $1/1024 "MB " $2}' | sort -nr

# ============================================
# التحقق من ملفات PEM أو KEY متتبعة
# ============================================
git ls-files | grep -E '\.(pem|key|secret)$'
```

### 3) أوامر PowerShell للفحص

```powershell
# ============================================
# أكبر 20 ملف متتبَّع في الـ working tree
# ============================================
git ls-files | ForEach-Object { 
    $file = Get-Item $_ -ErrorAction SilentlyContinue
    if ($file) { [PSCustomObject]@{ 
        Path = $_.FullName
        SizeMB = [math]::Round($file.Length / 1MB, 2) 
    }}
} | Sort-Object SizeMB -Descending | Select-Object -First 20

# ============================================
# أكبر 20 مجلد (top level) حسب الحجم التقريبي
# ============================================
Get-ChildItem -Directory | ForEach-Object {
    $size = (Get-ChildItem -Recurse -File $_.FullName -ErrorAction SilentlyContinue | 
             Measure-Object Length -Sum).Sum
    [PSCustomObject]@{ 
        Name = $_.Name
        SizeMB = [math]::Round($size / 1MB, 2) 
    }
} | Sort-Object SizeMB -Descending | Select-Object -First 20

# ============================================
# حجم المجلد الكلي (تقريبي)
# ============================================
$totalSize = (Get-ChildItem -Recurse -File -ErrorAction SilentlyContinue | 
              Measure-Object Length -Sum).Sum
Write-Host "Total size: $([math]::Round($totalSize / 1MB, 2)) MB"

# ============================================
# البحث عن ملفات > 5MB متتبعة في Git
# ============================================
git ls-files | ForEach-Object {
    $file = Get-Item $_ -ErrorAction SilentlyContinue
    if ($file -and $file.Length -gt 5MB) {
        [PSCustomObject]@{ 
            Path = $file.FullName
            SizeMB = [math]::Round($file.Length / 1MB, 2) 
        }
    }
} | Sort-Object SizeMB -Descending

# ============================================
# التحقق من ملفات PEM أو KEY متتبعة
# ============================================
git ls-files | Where-Object { $_ -match '\.(pem|key|secret)$' }
```

### 4) توصيات عند اكتشاف ملفات كبيرة

إذا ظهر أي ملف **> 50MB** متتبَّع في Git وكان **ليس سورسًا**:

#### الخطوات الموصى بها:

1. **إضافته إلى `.gitignore`**:
   ```bash
   echo "path/to/large-file.ext" >> .gitignore
   ```

2. **إخراجه من Git (مع إبقائه محليًا)**:
   ```bash
   git rm --cached path/to/large-file.ext
   ```

3. **إذا كان الملف موجودًا في التاريخ** (Git history):
   - استخدام `git filter-branch` أو `git filter-repo` (أكثر أمانًا)
   - **تحذير**: هذا يغير التاريخ ويحتاج `force push`
   - يُفضل تنفيذ هذا في فرع منفصل أولاً

#### مثال لكشف وإزالة ملف كبير:

```bash
# 1. اكتشاف الملف
git ls-files -z | xargs -0 du -k 2>/dev/null | awk '$1 > 50000 {print $1/1024 "MB " $2}' | sort -nr

# 2. إضافة إلى .gitignore
echo "path/to/large-file.ext" >> .gitignore

# 3. إخراج من Git (مع الإبقاء محليًا)
git rm --cached path/to/large-file.ext

# 4. Commit التغييرات
git add .gitignore
git commit -m "chore: remove large file from Git tracking"
```

---

## TERMINAL_COMMANDS (CHEATSHEET)

### Bash / Git Bash / WSL

```bash
# ============================================
# حجم المجلد الكلي (تقريبي)
# ============================================
du -sh .

# ============================================
# أكبر 15 مجلد في الجذر
# ============================================
du -sh * | sort -hr | head -n 15

# ============================================
# أكبر 10 ملفات في المجلد الحالي
# ============================================
find . -type f -exec du -k {} + | sort -nr | head -n 10

# ============================================
# عدد الملفات المتتبعة في Git
# ============================================
git ls-files | wc -l

# ============================================
# حجم الريبو في Git (حجم .git/)
# ============================================
du -sh .git/

# ============================================
# التحقق من ملفات .env متتبعة (لا يجب أن تكون)
# ============================================
git ls-files | grep '\.env'

# ============================================
# التحقق من ملفات build متتبعة (لا يجب أن تكون)
# ============================================
git ls-files | grep -E '(dist|build|\.next|coverage)/'
```

### PowerShell (Windows)

```powershell
# ============================================
# حجم المجلد الكلي (تقريبي)
# ============================================
$totalSize = (Get-ChildItem -Recurse -File -ErrorAction SilentlyContinue | 
              Measure-Object Length -Sum).Sum
Write-Host "Total size: $([math]::Round($totalSize / 1MB, 2)) MB"

# ============================================
# أكبر 15 مجلد في الجذر
# ============================================
Get-ChildItem -Directory | ForEach-Object {
    $size = (Get-ChildItem -Recurse -File $_.FullName -ErrorAction SilentlyContinue | 
             Measure-Object Length -Sum).Sum
    [PSCustomObject]@{ 
        Name = $_.Name
        SizeMB = [math]::Round($size / 1MB, 2) 
    }
} | Sort-Object SizeMB -Descending | Select-Object -First 15 | Format-Table -AutoSize

# ============================================
# أكبر 10 ملفات في المجلد الحالي
# ============================================
Get-ChildItem -Recurse -File -ErrorAction SilentlyContinue | 
    Sort-Object Length -Descending | 
    Select-Object -First 10 FullName, @{Name="SizeMB";Expression={[math]::Round($_.Length / 1MB, 2)}}

# ============================================
# عدد الملفات المتتبعة في Git
# ============================================
(git ls-files).Count

# ============================================
# حجم الريبو في Git (حجم .git/)
# ============================================
$gitSize = (Get-ChildItem -Recurse -File .git -ErrorAction SilentlyContinue | 
            Measure-Object Length -Sum).Sum
Write-Host "Git repo size: $([math]::Round($gitSize / 1MB, 2)) MB"

# ============================================
# التحقق من ملفات .env متتبعة (لا يجب أن تكون)
# ============================================
git ls-files | Where-Object { $_ -match '\.env' }

# ============================================
# التحقق من ملفات build متتبعة (لا يجب أن تكون)
# ============================================
git ls-files | Where-Object { $_ -match '(dist|build|\.next|coverage)[\\/]' }
```

### أوامر Git مفيدة

```bash
# ============================================
# التحقق من حالة Git (ملفات غير متتبعة)
# ============================================
git status

# ============================================
# رؤية الملفات المتتبعة في مجلد معين
# ============================================
git ls-files dist/
git ls-files node_modules/
git ls-files coverage/

# ============================================
# التحقق من حجم كل commit في التاريخ
# ============================================
git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1 == "blob" {print $3 " " $4}' | sort -nr | head -n 20
```

---

## EXEC_SUMMARY

### 1) تقييم الحجم الإجمالي للمشروع

✅ **الحجم ضمن المتوقع** لمشروع Monorepo NestJS:
- السورس الكود (~50-100 MB بدون dependencies) حجم معقول
- المشروع يحتوي على توثيق شامل (`docs/`) ومواصفات (`oas/`, `apps/`, `dashboards/`)
- لا توجد علامات على تضخم غير طبيعي في السورس

⚠️ **التحذيرات**:
- `node_modules/` (إذا تم تثبيته) قد يصل إلى ~200-400 MB - لكنه متجاهل بشكل صحيح ✅
- `dist/` قد يصل إلى ~20-50 MB - لكنه متجاهل بشكل صحيح ✅

### 2) أكبر أسباب تضخم الحجم المحتمل

| السبب | الحجم المتوقع | الحالة |
|-------|---------------|--------|
| `node_modules/` | ~200-400 MB | ✅ متجاهل |
| `dist/` | ~20-50 MB | ✅ متجاهل |
| `.nx/` (cache) | ~10-100 MB | ⚠️ **غير متجاهل** - يجب إضافته |
| `coverage/` | ~5-20 MB | ✅ متجاهل |
| Playwright artifacts | ~5-50 MB | ⚠️ **غير متجاهل** - يجب إضافته |

### 3) جودة `.gitignore` الحالي

⚠️ **جيد لكن يحتاج تحسين** (6.5/10)

**الإيجابيات**:
- ✅ يتعامل مع الأساسيات بشكل صحيح (`dist/`, `node_modules/`, `coverage/`)
- ✅ يتعامل مع ملفات `.env` الأساسية
- ✅ يتعامل مع ملفات OS والـ IDEs

**المشاكل الحرجة**:
- 🔴 **نقص أمني**: `*.pem`, `*.key` غير موجودة
- 🟠 **نقص مهم**: `.nx/` غير موجود (Nx مستخدم في المشروع)
- 🟠 **نقص متوسط**: Playwright artifacts غير موجودة

### 4) التوصية للانتقال إلى Source Tree Organizer

#### ⚠️ **يُنصح بإجراء التحسينات التالية قبل الانتقال**:

1. **أولوية عالية** (أمني):
   - إضافة `*.pem`, `*.key`, `*.secret` إلى `.gitignore`
   - التحقق من عدم وجود ملفات PEM/KEY متتبعة في Git:
     ```bash
     git ls-files | grep -E '\.(pem|key|secret)$'
     ```

2. **أولوية متوسطة** (حجم الريبو):
   - إضافة `.nx/` إلى `.gitignore`
   - إضافة Playwright artifacts (`test-results/`, `playwright-report/`)
   - إضافة `*.tsbuildinfo` بشكل عام

3. **أولوية منخفضة** (تحسينات):
   - إكمال أنماط `.env.*`
   - إضافة أنماط للمستقبل (`.turbo/`, `.expo/`, `build/`, `out/`)

#### ✅ **بعد تنفيذ التحسينات أعلاه**:

**من الآمن الانتقال إلى**:
> **BThwani — Source Tree Organizer (v1.4)**

#### 📋 **الخطوات الموصى بها**:

1. **الخطوة 1**: تحديث `.gitignore` بالمقترحات أعلاه
2. **الخطوة 2**: التحقق من عدم وجود ملفات كبيرة متتبعة:
   ```bash
   # Bash
   git ls-files -z | xargs -0 du -k 2>/dev/null | awk '$1 > 50000 {print $1/1024 "MB " $2}' | sort -nr
   
   # PowerShell
   git ls-files | ForEach-Object {
       $file = Get-Item $_ -ErrorAction SilentlyContinue
       if ($file -and $file.Length -gt 50MB) {
           Write-Host "$([math]::Round($file.Length / 1MB, 2)) MB - $($file.FullName)"
       }
   }
   ```
3. **الخطوة 3**: إذا وجدت ملفات كبيرة:
   - إضافتها إلى `.gitignore`
   - إخراجها من Git باستخدام `git rm --cached`
4. **الخطوة 4**: بعد التأكد من كل شيء، يمكن الانتقال إلى **Source Tree Organizer**

### 5) الملخص التنفيذي

| العنصر | التقييم | الإجراء المطلوب |
|--------|---------|-----------------|
| حجم السورس | ✅ جيد | لا شيء |
| `.gitignore` | ⚠️ يحتاج تحسين | إضافة أنماط مفقودة |
| الأمان | 🔴 يحتاج اهتمام | إضافة `*.pem`, `*.key` |
| حجم الريبو المحتمل | ⚠️ قد يتضخم | إضافة `.nx/` و Playwright artifacts |
| **جاهزية للتنظيم** | ⚠️ **بعد التحسينات** | **تنفيذ التحسينات أولاً** |

---

## الملاحظات الختامية

- **هذا التقرير تحليلي واقتراحي فقط** - لم يتم تنفيذ أي أوامر Git أو تعديل أي ملفات
- **جميع الأوامر المقترحة** يجب تنفيذها يدويًا من قبل المستخدم
- **يُنصح بشدة** بتنفيذ التحسينات الأمنية (`*.pem`, `*.key`) قبل أي شيء آخر
- **بعد تنفيذ التحسينات**, يمكن الانتقال بأمان إلى **BThwani — Source Tree Organizer (v1.4)**

---

**تاريخ الإنشاء**: `PLACEHOLDER_DATE`  
**المدقق**: Repo Size & GitIgnore Auditor (v1.0)

